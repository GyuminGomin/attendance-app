# Modules

## vue-tsc
```
Vue SFC(.vue 파일)을 대상으로 정적 타입 검사(Type Checking)를 수행하는 TypeScript CLI 도구

Vue 전용 TypeScript 컴파일러(타입체커)
-> .vue 파일까지 TypeScript 타입 검사에 포함시켜주는 도구

기존 tsc(typescript compiler)는 .ts파일만 검사하고 .vue파일은 검사하지 못하므로
vue-tsc를 쓰면 .vue파일의 타입 검사까지 정확하게 수행
```